import React, { useRef, useState } from 'react';
import { useQueryClient } from '@tanstack/react-query';
import { AdminGate } from '../components/admin-gate';
import { keys, uploadImage, useCreateCode, useDeleteCode, useFinalizeDay, useGetTodayStats, useListActiveCodes, useMarkCodeWon, useStartNewDay, markCodeCashOut } from '../lib/api';

function WinDialog({ codeId, onClose, onWin }: { codeId: number; onClose: () => void; onWin: (id: number, imageUrl: string | null) => void; }) {
  const fileRef = useRef<HTMLInputElement>(null);
  const [uploading, setUploading] = useState(false);
  const [preview, setPreview] = useState<string | null>(null);
  const [objectPath, setObjectPath] = useState<string | null>(null);
  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => { const file = e.target.files?.[0]; if (!file) return; setPreview(URL.createObjectURL(file)); setUploading(true); try { setObjectPath(await uploadImage(file)); } catch { alert('فشل رفع الصورة'); } finally { setUploading(false); } };
  return <div className="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 flex items-center justify-center p-4" dir="rtl"><div className="bg-[#0f1a0f] border border-green-700/50 rounded-2xl w-full max-w-md p-6 space-y-5"><div className="flex items-center justify-between"><h3 className="text-lg font-black text-white">✅ تأكيد الكسب</h3><button onClick={onClose} className="text-gray-500 hover:text-white text-xl">✕</button></div><div><p className="text-sm font-bold text-gray-400 mb-3">📸 ارفع صورة إثبات الربح (اختياري)</p>{preview && <img src={preview} className="w-full rounded-xl mb-3" />}<button onClick={() => fileRef.current?.click()} disabled={uploading} className="w-full border border-dashed border-green-700/40 text-green-400 font-bold py-3 rounded-xl">{uploading ? '⏳ جاري الرفع...' : preview ? '🔄 تغيير الصورة' : '📂 اختر صورة'}</button><input ref={fileRef} type="file" accept="image/*" className="hidden" onChange={handleFileChange} /></div><div className="flex gap-3"><button onClick={onClose} className="flex-1 border border-gray-700 text-gray-400 font-bold py-3 rounded-xl">إلغاء</button><button onClick={() => onWin(codeId, objectPath)} disabled={uploading} className="flex-1 bg-green-500 hover:bg-green-400 text-black font-black py-3 rounded-xl">✅ تأكيد الكسب</button></div></div></div>;
}

function CashOutDialog({ codeId, onClose, onCashOut }: { codeId: number; onClose: () => void; onCashOut: (id: number, imageUrl: string | null) => void; }) {
  const fileRef = useRef<HTMLInputElement>(null);
  const [uploading, setUploading] = useState(false);
  const [preview, setPreview] = useState<string | null>(null);
  const [objectPath, setObjectPath] = useState<string | null>(null);
  const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => { const file = e.target.files?.[0]; if (!file) return; setPreview(URL.createObjectURL(file)); setUploading(true); try { setObjectPath(await uploadImage(file)); } catch { alert('فشل رفع الصورة'); } finally { setUploading(false); } };
  return <div className="fixed inset-0 bg-black/80 backdrop-blur-sm z-50 flex items-center justify-center p-4" dir="rtl"><div className="bg-[#0f1a0f] border border-blue-700/50 rounded-2xl w-full max-w-md p-6 space-y-5"><div className="flex items-center justify-between"><h3 className="text-lg font-black text-white">📥 تأكيد الاستيراد</h3><button onClick={onClose} className="text-gray-500 hover:text-white text-xl">✕</button></div><div><p className="text-sm font-bold text-gray-400 mb-3">📸 ارفع صورة إثبات الاستيراد</p>{preview && <img src={preview} className="w-full rounded-xl mb-3" />}<button onClick={() => fileRef.current?.click()} disabled={uploading} className="w-full border border-dashed border-blue-700/40 text-blue-400 font-bold py-3 rounded-xl">{uploading ? '⏳ جاري الرفع...' : preview ? '🔄 تغيير الصورة' : '📂 اختر صورة'}</button><input ref={fileRef} type="file" accept="image/*" className="hidden" onChange={handleFileChange} /></div><div className="flex gap-3"><button onClick={onClose} className="flex-1 border border-gray-700 text-gray-400 font-bold py-3 rounded-xl">إلغاء</button><button onClick={() => onCashOut(codeId, objectPath)} disabled={uploading} className="flex-1 bg-blue-500 hover:bg-blue-400 text-black font-black py-3 rounded-xl">📥 تأكيد الاستيراد</button></div></div></div>;
}

function DayStats({ stats, codes, onFinalize, onNewDay, finalizing, startingNew }: any) {
  const totalCodes = parseFloat(stats?.totalCodes || '0');
  const wonCount = parseFloat(stats?.wonCodesCount || '0');
  const sumOdds = codes.reduce((acc: number, c: any) => acc + parseFloat(c.odds || '0'), 0);
  return <div className="bg-gradient-to-br from-[#0f1a0f] to-[#0a120a] border border-green-900/40 rounded-2xl p-6 space-y-5"><div className="flex items-center justify-between"><h3 className="font-black text-lg text-white">📊 إحصائيات اليوم</h3>{stats?.isFinalized && <span className="bg-green-500/20 border border-green-500/40 text-green-400 text-xs font-bold px-2 py-0.5 rounded-full">مكتمل</span>}</div><div className="flex flex-col gap-3"><div className="flex items-center justify-between bg-black/30 rounded-xl px-5 py-3 border border-green-900/30"><div className="text-sm text-gray-400 font-bold">إجمالي الأكواد</div><div className="text-2xl font-black text-white">{totalCodes}</div></div><div className="flex items-center justify-between bg-black/30 rounded-xl px-5 py-3 border border-green-900/30"><div className="text-sm text-gray-400 font-bold">الأكواد الرابحة</div><div className="text-2xl font-black text-green-400">{wonCount}</div></div><div className="flex items-center justify-between bg-black/30 rounded-xl px-5 py-3 border border-yellow-900/30"><div className="text-sm text-gray-400 font-bold">مجموع ربح الأكواد</div><div className="text-2xl font-black text-yellow-400">{sumOdds.toFixed(2)}</div></div></div>{totalCodes > 0 && <div className="bg-green-500/10 border border-green-500/30 rounded-xl p-4 text-center"><p className="text-sm text-gray-400 mb-1">مثال الربح — لو رميت 1000 جنيه على أكواد النهاردة</p><p className="text-2xl font-black text-green-400">{Math.round(1000 * sumOdds).toLocaleString('en-US')} جنيه</p><p className="text-xs text-gray-600">= 1000 × {sumOdds.toFixed(4)}</p></div>}<div className="flex gap-3 flex-col sm:flex-row">{!stats?.isFinalized ? <button onClick={onFinalize} disabled={finalizing || totalCodes === 0} className="flex-1 bg-yellow-500 hover:bg-yellow-400 disabled:opacity-50 text-black font-black py-3 rounded-xl text-sm">{finalizing ? '⏳ جاري الحساب...' : '🌙 نهاية اليوم - احسب الإحصائيات'}</button> : <button onClick={onNewDay} disabled={startingNew} className="flex-1 bg-green-500 hover:bg-green-400 disabled:opacity-50 text-black font-black py-3 rounded-xl text-sm">{startingNew ? '⏳ جاري البدء...' : '🌅 بداية يوم جديد'}</button>}</div></div>;
}

export default function AdminPage() {
  const queryClient = useQueryClient();
  const { data: codes, isLoading } = useListActiveCodes();
  const { data: todayStats } = useGetTodayStats();
  const createCode = useCreateCode();
  const markWon = useMarkCodeWon();
  const deleteCode = useDeleteCode();
  const finalizeDay = useFinalizeDay();
  const startNewDay = useStartNewDay();
  const [showForm, setShowForm] = useState(false);
  const [winDialog, setWinDialog] = useState<number | null>(null);
  const [cashOutDialog, setCashOutDialog] = useState<number | null>(null);
  const [form, setForm] = useState({ tipOutcome: '', tipCode: '', odds: '' });
  const [codeImageUploading, setCodeImageUploading] = useState(false);
  const [codeImagePreview, setCodeImagePreview] = useState<string | null>(null);
  const [codeImagePath, setCodeImagePath] = useState<string | null>(null);
  const codeImageRef = useRef<HTMLInputElement>(null);

  const invalidate = () => [keys.active, keys.todayStats, keys.yesterdayStats, keys.won, keys.stats30].forEach((k) => queryClient.invalidateQueries({ queryKey: k as any }));
  const handleCodeImageChange = async (e: React.ChangeEvent<HTMLInputElement>) => { const file = e.target.files?.[0]; if (!file) return; setCodeImagePreview(URL.createObjectURL(file)); setCodeImageUploading(true); try { setCodeImagePath(await uploadImage(file)); } catch { alert('فشل رفع الصورة'); } finally { setCodeImageUploading(false); } };
  const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); if (!form.tipCode || !form.odds) return alert('املأ الكود ونسبة الربح'); try { await createCode.mutateAsync({ tipOutcome: form.tipOutcome, tipCode: form.tipCode, odds: parseFloat(form.odds), codeImageUrl: codeImagePath }); setForm({ tipOutcome: '', tipCode: '', odds: '' }); setCodeImagePreview(null); setCodeImagePath(null); setShowForm(false); invalidate(); alert('تم إضافة الكود'); } catch { alert('فشل إضافة الكود'); } };
  const handleWin = async (id: number, imageUrl: string | null) => { try { await markWon.mutateAsync({ id, proofImageUrl: imageUrl }); setWinDialog(null); invalidate(); alert('تم تحديد الكود كرابح'); } catch { alert('فشل تحديث الكود'); } };
  const handleDelete = async (id: number) => { if (!confirm('هل أنت متأكد من حذف هذا الكود؟')) return; try { await deleteCode.mutateAsync({ id }); invalidate(); } catch { alert('فشل حذف الكود'); } };
  const handleCashOut = async (id: number, imageUrl: string | null) => { try { await markCodeCashOut(id, imageUrl); setCashOutDialog(null); invalidate(); alert('تم الاستيراد'); } catch { alert('فشل الاستيراد'); } };
  const handleFinalize = async () => { try { await finalizeDay.mutateAsync(); invalidate(); alert('تم حساب إحصائيات اليوم'); } catch { alert('فشل حساب الإحصائيات'); } };
  const handleNewDay = async () => { try { await startNewDay.mutateAsync(); invalidate(); alert('تم بدء يوم جديد'); } catch { alert('فشل بدء اليوم الجديد'); } };

  return <AdminGate><div className="space-y-8 max-w-4xl mx-auto" dir="rtl">{winDialog && <WinDialog codeId={winDialog} onClose={() => setWinDialog(null)} onWin={handleWin} />}{cashOutDialog && <CashOutDialog codeId={cashOutDialog} onClose={() => setCashOutDialog(null)} onCashOut={handleCashOut} />}<div className="flex items-center justify-between"><div><h1 className="text-2xl md:text-3xl font-black text-white">⚙️ لوحة الإدارة</h1><p className="text-gray-500 text-sm mt-1">إضافة وإدارة أكواد التوقعات اليومية</p></div><button onClick={() => setShowForm(!showForm)} className="flex items-center gap-2 bg-green-500 hover:bg-green-400 text-black font-black px-4 py-2 rounded-xl text-sm">{showForm ? '✕ إغلاق' : '➕ إضافة كود'}</button></div>{showForm && <div className="bg-gradient-to-br from-[#0f1a0f] to-[#0a120a] border border-green-700/50 rounded-2xl p-6"><h3 className="font-black text-lg text-white mb-5">➕ إضافة كود جديد</h3><form onSubmit={handleSubmit} className="space-y-4"><div className="grid grid-cols-2 gap-4"><div><label className="text-xs font-bold text-gray-400 mb-2 block">نوع التوقع</label><input value={form.tipOutcome} onChange={(e) => setForm({ ...form, tipOutcome: e.target.value })} placeholder="مثال: 1 أو X2" className="w-full bg-black/50 border border-green-900/50 rounded-xl px-4 py-3 text-white" /></div><div><label className="text-xs font-bold text-gray-400 mb-2 block">نسبة ربح الكود</label><input value={form.odds} onChange={(e) => setForm({ ...form, odds: e.target.value })} placeholder="مثال: 1.75" type="number" step="0.001" min="1" className="w-full bg-black/50 border border-green-900/50 rounded-xl px-4 py-3 text-white" /></div></div><div><label className="text-xs font-bold text-gray-400 mb-2 block">الكود</label><input value={form.tipCode} onChange={(e) => setForm({ ...form, tipCode: e.target.value })} placeholder="مثال: DQU6X" className="w-full bg-black/50 border border-green-900/50 rounded-xl px-4 py-3 text-white font-mono text-lg tracking-widest" /></div><div><label className="text-xs font-bold text-gray-400 mb-2 block">صورة الكود</label><input ref={codeImageRef} type="file" accept="image/*" onChange={handleCodeImageChange} className="hidden" /><button type="button" onClick={() => codeImageRef.current?.click()} disabled={codeImageUploading} className="w-full border-2 border-dashed border-green-900/50 rounded-xl p-4 text-center text-gray-500">{codeImageUploading ? '⏳ جاري الرفع...' : codeImagePreview ? '🔄 تغيير الصورة' : '📸 رفع صورة للكود'}</button>{codeImagePreview && <div className="mt-3 relative"><img src={codeImagePreview} className="w-full rounded-xl h-auto" />{codeImagePath && <div className="mt-1 text-xs text-green-400 text-center">✅ تم الرفع بنجاح</div>}</div>}</div><div className="flex gap-3"><button type="button" onClick={() => setShowForm(false)} className="flex-1 border border-gray-700 text-gray-400 font-bold py-3 rounded-xl">إلغاء</button><button type="submit" disabled={createCode.isPending} className="flex-1 bg-green-500 hover:bg-green-400 disabled:opacity-60 text-black font-black py-3 rounded-xl">{createCode.isPending ? '⏳ جاري الإضافة...' : '✅ إضافة الكود'}</button></div></form></div>}<DayStats stats={todayStats} codes={codes || []} onFinalize={handleFinalize} onNewDay={handleNewDay} finalizing={finalizeDay.isPending} startingNew={startNewDay.isPending} /><div><h2 className="text-xl font-black text-white mb-4">📋 أكواد اليوم النشطة ({codes?.length || 0})</h2>{isLoading ? <div className="space-y-3">{[...Array(3)].map((_, i) => <div key={i} className="bg-[#0f1a0f] border border-green-900/30 rounded-xl h-20 animate-pulse" />)}</div> : codes && codes.length > 0 ? <div className="space-y-3">{codes.map((code: any) => <div key={code.id} className="bg-gradient-to-br from-[#0f1a0f] to-[#0a120a] border border-green-900/40 rounded-xl p-4 flex flex-col sm:flex-row items-start sm:items-center gap-4"><div className="flex-1 min-w-0 w-full"><div className="flex items-center gap-3 flex-wrap mb-2">{code.tipOutcome && <span className="bg-green-500/20 border border-green-500/40 text-green-400 font-black text-sm px-2 py-0.5 rounded">{code.tipOutcome}</span>}<span className="font-black text-white font-mono text-lg tracking-widest">{code.tipCode}</span><span className="text-yellow-400 font-bold text-sm">x{parseFloat(code.odds).toFixed(2)}</span></div>{code.codeImageUrl && <img src={code.codeImageUrl} alt="صورة الكود" className="w-full h-auto rounded-xl mb-2" style={{ border: '1px solid rgba(34,197,94,0.2)', maxHeight: '200px', objectFit: 'contain' }} /> }<div className="flex items-center gap-2 flex-wrap"><button onClick={() => setWinDialog(code.id)} className="bg-green-500/20 hover:bg-green-500/30 border border-green-500/40 text-green-400 font-bold text-xs px-3 py-2 rounded-lg">✅ كسب</button><button onClick={() => setCashOutDialog(code.id)} className="bg-blue-500/20 hover:bg-blue-500/30 border border-blue-500/40 text-blue-400 font-bold text-xs px-3 py-2 rounded-lg">📥 استيراد</button><button onClick={() => handleDelete(code.id)} className="bg-red-500/10 hover:bg-red-500/20 border border-red-500/30 text-red-400 font-bold text-xs px-3 py-2 rounded-lg">🗑️ حذف</button></div></div></div>)}</div> : <div className="text-center py-10 bg-[#0f1a0f]/50 border border-dashed border-green-900/30 rounded-2xl"><p className="text-gray-500">لا توجد أكواد نشطة. أضف أكواداً جديدة!</p></div>}</div></div></AdminGate>;
}
